#include<stdio.h>
#include<conio.h>
void main(){
int a,b,c;
printf("enter three number\n");
scanf("%d%d%d",&a,&b,&c);
{if(a>b)
printf("%d is the largest number",a);
else
printf("%d is the largest number",b);
}
if(b>c)

printf("%d is the larges number",b);
else
printf("%d is the largest number",c);

}
